import React, { Component } from 'react';

import { PageContainer } from '../ui';

export class Payment extends Component {
  render() {
    return (
      <PageContainer>
        Payment
      </PageContainer>
    );
  }
}
